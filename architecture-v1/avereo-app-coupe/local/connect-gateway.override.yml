services:
  web:
    environment:
      APP_LAUNCH_COUPE_URL: http://127.0.0.1:8200/connect/entry.php
      APP_LAUNCH_COUPE_SECRET: ${COUPE_CONNECT_GATEWAY_SECRET:?COUPE_CONNECT_GATEWAY_SECRET is required}
