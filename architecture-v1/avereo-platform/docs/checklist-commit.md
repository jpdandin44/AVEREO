# Checklist commit

- Verifier `git status`.
- Verifier qu'aucun secret n'est present.
- Verifier qu'aucun `.env`, `node_modules/` ou `frontend/dist/` n'est versionne.
- Lancer les validations possibles.
