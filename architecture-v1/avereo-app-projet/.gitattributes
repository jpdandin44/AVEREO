# Conserver les octets du CSV (BOM UTF-8 et CRLF) contrôlés par le générateur.
data/generated/*.csv -text whitespace=cr-at-eol
# Le JSON généré reste identique sur Windows et Linux.
data/generated/*.json text eol=lf
